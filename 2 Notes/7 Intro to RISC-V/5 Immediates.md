This is like the `add` command, but instead of adding the value of 2 registers, it's adding the value of 1 register with a constant.

![[Screenshot 2026-02-12 at 3.21.29 PM.png|500]]

Very similar syntax to the add instruction, just instead of a register, we have a constant.
- and also, there isn't `subi` in assembly, because you simply just add negative values with `addi`.