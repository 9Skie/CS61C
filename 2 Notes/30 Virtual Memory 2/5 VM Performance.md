Virtual Memory is the level of the memory hierarchy that sits below main memory.
- The TLB comes before cache, but affects transfer of data from disk to main memory

So, the same cache formulas apply to VM as well, but now we treat main memory like a mid-level cache.